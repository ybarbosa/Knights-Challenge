{
  "name": "desafio-btg",
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "private": true,
  "scripts": {
    "start:backend": "yarn workspace backend start:dev",
    "prisma:generate": "yarn workspace backend prisma:generate",
    "prisma:push": "yarn workspace backend prisma:push"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
